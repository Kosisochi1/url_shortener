<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="../public/signup.css" />
		<title>Document</title>
	</head>
	<body>
		<header><%- include('./partials/headers'); %></header>

		<main>
			<!-- <% Data.forEach(function(history){%>
			<p><%= history %></p>

			<%})%> -->

			<%if(Data.length >= 0){%> <% Data.forEach(function(view_history){%>
			<div class="">
				<li>
					<p><strong>Short Url: </strong><%= view_history.Short_url_link%></p>
					<a href="/analytic/<%=view_history._id%>">View Details</a>
				</li>
				<!-- <p>Time Created:<%= view_history.createdAt%></p>
				<p>Time Created:<%= view_history._id%></p>
				<img src="<%=view_history.Qr_code%>" alt="QR Code" /> -->
			</div>
			<%})%> <%}%>
		</main>
		<footer><%- include('./partials/footer'); %></footer>
	</body>
</html>
